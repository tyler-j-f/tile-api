spring:
  application:
    token-config:
      base_name: "Tile #"
      description: "Tile NFT. Buy a tile and customize it yourself!!! Each tile will be generated with unique traits that decide how rare your Tile is."
      base_external_url_postfix: "/?tokenId="
      base_image_url_postfix: "/api/image/tile/get/"
      background_color: null # maybe we should add a background_color?
      base_animation_url: null
      base_youtube_url: null
      # attributes key will be decided by *token-config.yaml
